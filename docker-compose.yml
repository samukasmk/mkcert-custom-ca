services:
  proxy:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./certs/localhost:/etc/nginx/certs
    extra_hosts:
      - "host.docker.internal:host-gateway"
